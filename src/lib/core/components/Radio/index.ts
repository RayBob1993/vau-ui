import { default as Root } from './RadioRoot.vue';
import { default as Indicator } from './RadioIndicator.vue';
import { default as Title } from './RadioTitle.vue';
import { default as Group } from './RadioGroup.vue';

export const Radio = {
  Root,
  Indicator,
  Title,
  Group
};

export * from './types';
