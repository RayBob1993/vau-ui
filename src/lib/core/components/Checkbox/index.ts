import { default as Root } from './CheckboxRoot.vue';
import { default as Indicator } from './CheckboxIndicator.vue';
import { default as Title } from './CheckboxTitle.vue';
import { default as Group } from './CheckboxGroup.vue';

export const Checkbox = {
  Root,
  Indicator,
  Title,
  Group
};

export * from './types';
