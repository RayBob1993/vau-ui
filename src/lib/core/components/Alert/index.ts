import { default as Root } from './AlertRoot.vue';
import { default as Header } from './AlertHeader.vue';
import { default as Indicator } from './AlertIndicator.vue';
import { default as Title } from './AlertTitle.vue';
import { default as Body } from './AlertBody.vue';
import { default as Description } from './AlertDescription.vue';
import { default as Close } from './AlertClose.vue';

export const Alert = {
  Root,
  Header,
  Indicator,
  Title,
  Body,
  Description,
  Close
};

export * from './types';
