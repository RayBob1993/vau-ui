import { default as Root } from './SwitchRoot.vue';
import { default as Indicator } from './SwitchIndicator.vue';
import { default as Title } from './SwitchTitle.vue';

export const Switch = {
  Root,
  Indicator,
  Title
};

export * from './types';
