import { default as Root } from './ModalRoot.vue';
import { default as Body } from './ModalBody.vue';
import { default as Close } from './ModalClose.vue';
import { default as Header } from './ModalHeader.vue';
import { default as Footer } from './ModalFooter.vue';
import { default as Title } from './ModalTitle.vue';
import { default as Content } from './ModalContent.vue';

export const Modal = {
  Root,
  Body,
  Close,
  Header,
  Footer,
  Title,
  Content
};

export * from './types';
