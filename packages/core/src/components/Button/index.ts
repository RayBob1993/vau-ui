import { default as Root } from './ButtonRoot.vue';
import { default as Content } from './ButtonContent.vue';
import { default as Group } from './ButtonGroup.vue';

export const Button = {
  Root,
  Content,
  Group
};

export * from './types';
