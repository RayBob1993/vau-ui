import { default as Root } from './SectionRoot.vue';
import { default as Header } from './SectionHeader.vue';
import { default as Title } from './SectionTitle.vue';
import { default as Footer } from './SectionFooter.vue';
import { default as Body } from './SectionBody.vue';

export const Section = {
  Root,
  Header,
  Title,
  Footer,
  Body
};

export * from './types';
